## 1. Payload Layout

- [ ] 1.1 Add `offline-bundle/payload/` as the generated/downloaded artifact root used by all bundle scripts.
- [ ] 1.2 Move K3s artifact paths from `offline-bundle/artifacts/k3s/` to `offline-bundle/payload/k3s/`.
- [ ] 1.3 Move Ansible `.deb` package paths from `offline-bundle/artifacts/debs/ubuntu-24.04-amd64/ansible-and-deps/` to `offline-bundle/payload/debs/ubuntu-26.04-amd64/ansible-and-deps/`.
- [ ] 1.4 Move checksum output and verification from `offline-bundle/checksums.txt` to `offline-bundle/payload/checksums.txt`.
- [ ] 1.5 Remove obsolete placeholder files and old generated artifact paths under `offline-bundle/artifacts/` after scripts and docs use `payload/`.

## 2. Git Ignore Rules

- [ ] 2.1 Update `.gitignore` to ignore only `offline-bundle/payload/` for offline bundle generated artifacts.
- [ ] 2.2 Remove individual ignore rules for old K3s binary, install script, air-gap tarball, version file, and `.deb` patterns once `payload/` is ignored.
- [ ] 2.3 Confirm `offline-bundle/scripts/`, `offline-bundle/ansible/`, `offline-bundle/README-offline.md`, and `offline-bundle/VALIDATION.md` remain trackable.

## 3. Script Updates

- [ ] 3.1 Update `download-k3s-artifacts.sh` to create and write K3s payload files under `payload/k3s/`.
- [ ] 3.2 Update `download-ansible-debs.sh` to require Ubuntu 26.04 and write `.deb` files under `payload/debs/ubuntu-26.04-amd64/ansible-and-deps/`.
- [ ] 3.3 Update shared checksum generation logic in download scripts to hash files under `payload/` and write `payload/checksums.txt`.
- [ ] 3.4 Update `verify-artifacts.sh` to validate required payload paths and `payload/checksums.txt`.
- [ ] 3.5 Update `install-ansible-offline.sh` to install local packages from the Ubuntu 26.04 payload `.deb` directory.

## 4. Ansible Updates

- [ ] 4.1 Update `offline-bundle/ansible/group_vars/all.yml` to read K3s artifacts from `payload/k3s/`.
- [ ] 4.2 Confirm the `k3s_offline` role still copies the payload K3s binary, install script, and air-gap image to the documented target paths.
- [ ] 4.3 Confirm conditional image import cache behavior remains unchanged after the path migration.

## 5. Documentation Updates

- [ ] 5.1 Update `README-offline.md` target and preparation environment references from Ubuntu 24.04 to Ubuntu 26.04.
- [ ] 5.2 Document that `offline-bundle/payload/` is ignored, can be deleted, and is regenerated by the download scripts.
- [ ] 5.3 Add a Docker command using `--platform linux/amd64`, `ubuntu:26.04`, and a mounted repo or bundle to download all payload files for offline use.
- [ ] 5.4 Update README artifact path examples to use `payload/k3s/`, `payload/debs/ubuntu-26.04-amd64/ansible-and-deps/`, and `payload/checksums.txt`.
- [ ] 5.5 Update `VALIDATION.md` to reflect the Ubuntu 26.04 Docker preparation flow and payload verification paths.

## 6. Validation

- [ ] 6.1 Run shell syntax checks for all bundle scripts.
- [ ] 6.2 Run Ansible syntax validation for the bundled playbook when Ansible is available.
- [ ] 6.3 Search active bundle files for stale `ubuntu-24.04`, `Ubuntu 24.04`, `artifacts/k3s`, `artifacts/debs`, and root `checksums.txt` references.
- [ ] 6.4 Record any validation that cannot be run locally and the environment needed to complete it.
